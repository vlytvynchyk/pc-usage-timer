<Window x:Class="PcUsageTimer.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" SizeToContent="WidthAndHeight"
        FontFamily="Segoe UI"
        Loaded="Window_Loaded">

    <Border Background="#232739" CornerRadius="8" Padding="18,12"
            BorderBrush="#373C48" BorderThickness="1" Margin="10"
            MinWidth="280">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#x23F0;" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>

            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Text="Time Warning" FontSize="14"
                           FontWeight="Bold" Foreground="#E6EBF5"/>
                <TextBlock x:Name="MessageText" Text="3 minutes remaining"
                           FontSize="12" Foreground="#788296" Margin="0,3,0,0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
